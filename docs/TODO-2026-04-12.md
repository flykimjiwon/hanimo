# hanimo 미완성 작업 통합 우선순위 — 2026-04-12

> 2026-04-11~12 세션에서 구현한 27+ 커밋 이후,
> 남은 작업을 가치/시간 기준으로 정리한 실행 큐.
> 완료 시 체크 ✅ 로 갱신.

---

## Tier 1 — 즉시 가치 (~2일)

- [ ] **#1 Knowledge 폴더 MVP** (3~4h)
  `.hanimo/knowledge/` 에 md/txt → FTS5 색인 + `knowledge_search` 도구 + `/knowledge` 슬래시
  사용자가 직접 요청. 가장 높은 우선순위.

- [ ] **#2 세션 복구 wire-up** (2~3h)
  `/save`, `/load`, `/search`, `--resume` 스텁 → 실제 동작.
  backend(session/db.go + store.go) 100% 구현됨, wire-up만 남음.

- [ ] **#3 SKILL.md 로더 MVP** (1d)
  `~/.hanimo/skills/*/SKILL.md` 스캔 + `/skill <name>` 호출.
  agentskills.io 호환. frontmatter(name/description/allowed-tools) + 본문 lazy load.

- [ ] **#4 Prompt caching** (4~6h)
  Anthropic `cache_control: {"type":"ephemeral"}` + OpenAI `prompt_cache_key`.
  시스템 프롬프트 invariant 부분에 cache breakpoint 삽입.
  비용 최대 90% 절감.

- [ ] **#5 Anthropic 네이티브 provider** (1d)
  현재 OpenAI-compat stub → `anthropic-sdk-go` 사용 `/v1/messages` 네이티브.
  Claude 4.6 cache/vision/tool_use 전부 열림.

## Tier 2 — 높은 가치 (~1.5일)

- [ ] **#6 Rate limit tracker** (4h)
  프로바이더별 429 상태 학습 + 지능적 백오프/자동 전환.
  참고: hermes-agent `agent/rate_limit_tracker.py`.

- [ ] **#7 Title generator** (2h)
  세션 자동 제목 생성 (LLM 2줄 요약). 세션 복구 UX 핵심.
  참고: hermes-agent `agent/title_generator.py`.

- [ ] **#8 Secret redaction** (3h)
  LLM 응답/로그에서 API key/토큰 자동 마스킹.
  참고: hermes-agent `agent/redact.py`.

- [ ] **#9 MCP protocolVersion 업그레이드** (5분)
  `2024-11-05` → `2025-06-18`. 최신 MCP 서버 기능 호환.
  위치: `internal/mcp/client.go:134`.

- [ ] **#10 Model list fallback** (30분)
  ListModels() 실패 시 cfg.Models.Super/Dev 표시.
  "모델 목록을 가져올 수 없습니다" 반복 제거.

## Tier 3 — 가치 있지만 급하지 않음 (~2일)

- [ ] **#11 Smart model routing** (4h) — 속도/비용/가용성 기반 자동 전환
- [ ] **#12 Memory system 활성화** (2h) — `/remember`, `/memories` wire-up
- [ ] **#13 Context compressor 개선** (4h) — hermes 기법 차용, 토큰 기반 트리거
- [ ] **#14 DebugMode 플래그화** (15분) — Makefile 하드코딩 제거
- [ ] **#15 Credential pool** (3h) — 여러 API 키 로테이션

## Tier 4 — 미래용 (선택)

- [ ] #16 Error classifier (3h)
- [ ] #17 Delegate tool / subagent (2~3d)
- [ ] #18 Knowledge 임베딩 RAG v2 (1~2d)
- [ ] #19 `.hanimoignore` / `.gitignore` 파서 (2h)
- [ ] #20 Repo-map lite (1d)

---

## 이미 완료 (2026-04-11~12 세션, 27 커밋)

- [x] list_tree 도구
- [x] 429/5xx 재시도 + 한국어 에러
- [x] 붙여넣기 + 멀티라인 collapse + /pastes
- [x] Doom loop 읽기 도구 예외
- [x] Dangerous regex 확장 + 자격증명 방어
- [x] Prompt embed 분리 (5 md 파일)
- [x] Read-before-write 강제
- [x] Baked distro/sealed 빌드 시스템
- [x] 자기 레포 감지 + DEV_MODE 바이패스
- [x] 라이브 스트리밍 렌더 + ticker
- [x] 스크롤 중 sticky bottom
- [x] Checkpoint/Rollback (git stash)
- [x] UTF-8 한글 깨짐 수정
- [x] ASK overlay 레이아웃 수정
- [x] Q/A 배경색 블록 + 세로 바
- [x] Persona 모듈 (포팅 교체용)
- [x] 자동 접기 tool result + diff 박스
- [x] 메뉴 감사 (테마 서브메뉴, 자율모드→Deep Agent)
- [x] 프로바이더 확장 + 현재 표시 + Custom URL
- [x] 언어 서브메뉴
- [x] 도구 목록 메뉴
- [x] 데스크탑 앱 플랜 문서 (590줄)
- [x] 토이 프로젝트 테스트 셋업 (hanimo-toy-coffee)

---

## 참고 소스

- `hermes-agent/` — `~/Desktop/kimjiwon/hermes-agent/` (2026-04-12 clone)
  - `agent/memory_manager.py` (362줄) — memory nudge
  - `agent/context_compressor.py` (777줄) — 3-stage compression
  - `agent/skill_commands.py` (368줄) — SKILL.md invoke
  - `agent/smart_model_routing.py` (195줄) — provider routing
  - `agent/prompt_caching.py` — cache control
  - `agent/rate_limit_tracker.py` — 429 학습
  - `agent/redact.py` — secret masking
  - `agent/title_generator.py` — session title
  - `tools/session_search_tool.py` — FTS5 search
  - `tools/skill_manager_tool.py` — skill registry

- `TECHAI_CODE/docs/superpowers/specs/` — Obsidian wiki-link 설계안 (코드 미구현)

---

_Last updated: 2026-04-12_
